import React from "react";
import ReactDOM from "react-dom";

const BCDrawer: React.FC = () => {
    return (
        <div>
            Hello World!
        </div>
    );
};

export default BCDrawer;
